<UserControl x:Class="pdfFiller.UI.AppStartUI.SocialAuth.SocialAuthControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:prop="clr-namespace:pdfFiller.AttachedProperties"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:local="clr-namespace:pdfFiller.UI.AppStartUI.SocialAuth"
             xmlns:wv2="clr-namespace:pdfFiller.UI.WebView"
             Background="White">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="40"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>
    <Border BorderBrush="#eee" BorderThickness="0 0 0 2" Grid.Row="0">
      <Grid>
        <Button Style="{StaticResource ButtonWithIconLeft}" Foreground="#444" Width="40" HorizontalAlignment="Left"
                FontFamily="{DynamicResource Medium}"
                prop:ButtonAttachedProperties.ButtonIcon="/pdfFiller;component/Resources/Images/back.png"
                BorderBrush="Transparent" Background="Transparent" Command="{Binding BackCommand}"/>
        <TextBlock x:Name="Title" FontSize="12" Foreground="#444" FontFamily="{DynamicResource Bold}"
                   HorizontalAlignment="Center" VerticalAlignment="Center" Text="{Binding Title}"/>
      </Grid>
    </Border>
    <wv2:WebView x:Name="Browser" Unloaded="Browser_Unloaded" Loaded="Browser_Loaded" Grid.Row="1"/>
  </Grid>
</UserControl>