<UserControl x:Class="pdfFiller.UI.JustFolders.JustFoldersControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:pdfFiller.UI.JustFolders"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:utils="clr-namespace:pdfFiller.Utils.View"
             xmlns:converters="clr-namespace:pdfFiller.Converters"
             MinWidth="280" Background="White">
  <i:Interaction.Triggers>
    <i:EventTrigger EventName="Loaded">
      <i:CallMethodAction MethodName="LoadData" TargetObject="{Binding}"/>
    </i:EventTrigger>
  </i:Interaction.Triggers>
  <Grid>
    <utils:ListViewSelectionItemChangedOnMouseUp ItemContainerStyle="{StaticResource FoldersListViewBase}"
                                                 ItemTemplate="{StaticResource FoldersTemplate}"
                                                 ItemTemplateSelector="{StaticResource FoldersTemplateDelegate}" BorderThickness="0"
                                                 BorderBrush="Transparent" ItemsSource="{Binding Folders}" SelectedItem="{Binding SelectedFolder}"/>
    <utils:Loader Visibility="{Binding IsLoading, Converter={StaticResource visibilityConverter}}"/>
  </Grid>
</UserControl>