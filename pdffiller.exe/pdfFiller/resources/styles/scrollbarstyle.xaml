<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Style x:Key="ThumbStyle" TargetType="{x:Type Thumb}">
    <Setter Property="UIElement.SnapsToDevicePixels" Value="true"/>
    <Setter Property="FrameworkElement.OverridesDefaultStyle" Value="true"/>
    <Setter Property="Control.IsTabStop" Value="false"/>
    <Setter Property="UIElement.Focusable" Value="false"/>
    <Setter Property="Control.Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type Thumb}">
          <Grid>
            <Rectangle x:Name="Rectangle1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Width="auto"
                       Height="auto" Fill="Transparent"/>
            <Border x:Name="Border" CornerRadius="4" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                    Width="7" Height="auto" Background="{TemplateBinding Control.Background}"/>
          </Grid>
          <ControlTemplate.Triggers>
            <Trigger Property="FrameworkElement.Tag" Value="Horizontal">
              <Setter TargetName="Rectangle1" Property="FrameworkElement.Width" Value="Auto"/>
              <Setter TargetName="Rectangle1" Property="FrameworkElement.Height" Value="7"/>
              <Setter TargetName="Border" Property="FrameworkElement.Width" Value="Auto"/>
              <Setter TargetName="Border" Property="FrameworkElement.Height" Value="7"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style TargetType="{x:Type ScrollBar}">
    <Setter Property="Stylus.IsFlicksEnabled" Value="false"/>
    <Setter Property="Control.Background" Value="White"/>
    <Setter Property="Control.Foreground" Value="#ddd"/>
    <Setter Property="FrameworkElement.Width" Value="10"/>
    <Setter Property="Control.Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type ScrollBar}">
          <Grid x:Name="GridRoot" Width="12" Focusable="false" Background="{x:Null}">
            <Track x:Name="PART_Track" IsDirectionReversed="true">
              <Track.Thumb>
                <Thumb x:Name="Thumb" Background="{TemplateBinding Control.Foreground}"
                       Style="{DynamicResource ThumbStyle}"/>
              </Track.Thumb>
            </Track>
          </Grid>
          <ControlTemplate.Triggers>
            <Trigger SourceName="Thumb" Property="UIElement.IsMouseOver" Value="true">
              <Setter TargetName="Thumb" Value="{DynamicResource ButtonSelectBrush}" Property="Control.Background"/>
            </Trigger>
            <Trigger SourceName="Thumb" Property="Thumb.IsDragging" Value="true">
              <Setter TargetName="Thumb" Value="{DynamicResource DarkBrush}" Property="Control.Background"/>
            </Trigger>
            <Trigger SourceName="Thumb" Property="UIElement.IsEnabled" Value="false">
              <Setter TargetName="Thumb" Property="UIElement.Visibility" Value="Collapsed"/>
            </Trigger>
            <Trigger Property="ScrollBar.Orientation" Value="Horizontal">
              <Setter TargetName="GridRoot" Property="FrameworkElement.LayoutTransform">
                <Setter.Value>
                  <RotateTransform Angle="-90"/>
                </Setter.Value>
              </Setter>
              <Setter TargetName="PART_Track" Property="FrameworkElement.LayoutTransform">
                <Setter.Value>
                  <RotateTransform Angle="-90"/>
                </Setter.Value>
              </Setter>
              <Setter Property="FrameworkElement.Width" Value="Auto"/>
              <Setter Property="FrameworkElement.Height" Value="12"/>
              <Setter TargetName="Thumb" Property="FrameworkElement.Tag" Value="Horizontal"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>